# Copy → .env.local for local overrides

# Mock UI (không cần backend).
# true  = luôn dùng mock
# false = luôn gọi API thật (cần VITE_API_BASE_URL)
# bỏ trống = mock tự bật nếu chưa có VITE_API_BASE_URL (phù hợp Vercel demo)
VITE_USE_MOCK=false

# BE local (Phase 1–2): Auth, Users/Roles, Categories, Products, Cart, Orders, Reviews
VITE_API_BASE_URL=http://localhost:5259

# Tài khoản test BE:
# Admin: ngthanhtrung302005@gmail.com / Admin@123
# Customer: khach.phase2@test.com / Khach@123
